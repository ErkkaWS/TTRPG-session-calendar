# Copy this file to "secrets.env", fill in your real values, and protect it:
#
#   cp secrets.env.example secrets.env
#   nano secrets.env
#   chmod 600 secrets.env
#
# NEVER share the filled-in secrets.env file with anyone, and never
# commit it to git (it's already excluded via .gitignore).

export DISCORD_WEBHOOK_SCHEDULE="https://discord.com/api/webhooks/..."
export DISCORD_WEBHOOK_ANNOUNCE="https://discord.com/api/webhooks/..."
